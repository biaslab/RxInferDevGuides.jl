module RxInferDevGuides

# This is not a real package, but we use Julia infrastructure to build the documentation.

# re-export LiveServer.jl `servedocs()` for easier documentation development
using LiveServer

export servedocs

end
